breaks:
  day:       # The day the breaks are on 0=Monday, 1=Tuesday etc
  time:      # The time the breaks are on, formatted as e.g. 1500
  location:  # Where the breaks take place
  maximum:   # The maximum number of breaks to allocate in the future
admin:
  name:
db:          # Database parameters
  host:
  user:
  database:
  password:
lists:        # Lists to send the emails to
log:          # File to write log messages in